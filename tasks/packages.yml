---

- name: Install yum packages
  yum:
    name:
      - curl
      - epel-release
      - gcc
      - gdb
      - htop
      - java-1.8.0-openjdk
      - net-tools
      - nmap
      - python-pip
      - rsync
      - screen
      - strace
      - telnet
      - vim
      - wget

- name: Install pip packages
  pip:
    name:
      - speedtest-cli

- name: Run geerlingguy.ntp
  include_role:
    name: geerlingguy.ntp

